# 前端页面功能总览

本文件整理当前前端页面、可访问角色、主要功能和关键交互，便于产品梳理、测试和后续迭代。

## 1. 页面总览

| 页面 | 路由 | 角色 | 主要用途 |
| --- | --- | --- | --- |
| 登录 | `/login` | 公共 | 选择本地 mock 角色并进入系统 |
| 仪表盘 | `/` | 已登录用户 | 查看概览与快捷入口 |
| 上传包裹 | `/upload` | `customer` / `staff` | 创建包裹并上传图片 |
| 包裹 | `/parcels` | `customer` / `staff` / `operator` | 查看包裹、推进状态、申请打包、查看通知 |
| 订单 | `/orders` | `customer` / `staff` / `operator` | 查看订单列表、筛选订单、进入详情 |
| 订单详情 | `/orders/:id` | `customer` / `staff` / `operator` | 查看下单信息、包裹清单、重量处理、完成打包 |
| 任务 | `/tasks` | `staff` / `operator` | 查看真实打包任务并开始处理 |
| 结算 | `/billing` | `staff` | 查看待结算订单、计价、人工覆盖价格 |
| 客户 | `/customers` | `staff` | 查看客户列表 |
| 客户详情 | `/customers/:id` | `staff` | 查看客户信息与关联订单 |

## 2. 公共能力

所有业务页面共享以下能力：

- 顶部栏：品牌、主题切换
- 登录后右上角角色菜单：切换角色、退出
- 左侧导航：根据角色显示不同页面入口
- 路由守卫：未登录跳转登录页，无权限角色跳回首页

## 3. 页面详细说明

## 3.1 登录页 `Login.vue`

主要功能：

- 选择角色：
  - 客户
  - 员工
  - 操作员
- 点击“进入系统”后写入本地 mock token 和角色

特点：

- 当前为演示登录
- 不做真实账号密码校验
- 未登录页隐藏左侧导航

## 3.2 仪表盘 `Dashboard.vue`

主要功能：

- 展示静态概览数据
- 按角色展示不同模块
- 提供客户视角的快捷入口

当前状态：

- 仍偏静态演示
- 后续建议改造成角色化工作台

## 3.3 上传包裹 `Upload.vue`

主要功能：

- 创建包裹
- 填写：
  - 客户 ID
  - 运单号
  - 快递公司
  - 品类
  - 物品描述
  - 备注
- 上传图片
- 预览已选择文件

交互规则：

- `customer` 角色自动绑定自己的客户 ID
- `staff` 可手动填写客户 ID
- 运单号必填且需唯一
- 图片最多 5 张
- 单张最大 5MB
- 支持 `png / jpeg / webp`

## 3.4 包裹页 `Parcels.vue`

按角色分能力。

### 用户视角

主要功能：

- 搜索运单号
- 按状态筛选
- 查看自己的包裹
- 进入“选择打包”模式
- 勾选已到仓包裹
- 弹出转运信息窗口
- 提交打包申请
- 本地保存转运信息草稿
- 查看打包提示

关键字段：

- 运单号
- 状态
- 备注
- 提示

### 操作员视角

主要功能：

- 搜索运单号
- 按状态筛选
- 查看包裹列表
- 标记到仓
- 查看通知中心
- 刷新通知
- 清空打包申请通知
- 清空打包完成通知

通知区内容：

- 客户请求打包
- 客户打包完毕

### 员工视角

主要功能：

- 搜索运单号
- 按状态筛选
- 按客户筛选
- 查看所有包裹

## 3.5 订单页 `Orders.vue`

按角色展示不同订单数据，但结构基本一致。

主要功能：

- 搜索关联包裹运单号
- 按客户搜索
- 按订单状态筛选
- 按创建时间筛选
- 排序
- 查看订单列表
- 进入订单详情

操作功能：

- 对 `READY_TO_PACK` 订单开始打包
- 调整订单包裹

主要字段：

- 下单时间
- 订单号
- 客户
- 包裹数
- 状态
- 实重
- 体积重
- 最终价格

## 3.6 订单详情页 `OrderDetail.vue`

主要功能：

- 查看订单状态摘要
- 查看客户信息
- 查看包裹清单
- 查看转运信息
- 保存实际重量
- 计算并保存体积重量
- 完成打包并通知客户

页面区域：

- 订单状态摘要
- 转运信息
- 包裹表格
- 实际重量填写区
- 体积重量计算区
- 费用摘要
- 下一步动作区

关键按钮：

- 返回订单列表
- 保存实重
- 计算并保存体积重
- 打包完毕并通知客户

## 3.7 任务页 `Tasks.vue`

主要功能：

- 从 `/tasks` 拉取真实任务
- 按状态分栏：
  - 待办
  - 进行中
  - 已完成
- 查看订单号、客户、包裹数、实际重量

操作功能：

- `operator` 可在待办栏点击“开始处理”
- 可跳转订单详情继续填写重量和完成任务

## 3.8 结算页 `Billing.vue`

主要功能：

- 查看已完成订单
- 查看待结算统计：
  - 待结算订单数
  - 待结算金额
  - 平均订单重量
  - 人工覆盖次数
- 选择订单进行计价
- 输入实际重量、单价、额外费用
- 自动计算价格
- 人工覆盖最终价格
- 查看人工覆盖记录

当前特点：

- 覆盖记录保存在前端本地存储
- 仅作为 MVP 演示，不是正式账务系统

## 3.9 客户列表 `Customers.vue`

主要功能：

- 搜索客户名
- 查看客户列表
- 进入客户详情

## 3.10 客户详情 `CustomerDetail.vue`

主要功能：

- 查看客户名称
- 查看客户关联订单
- 跳转到订单详情

## 4. 页面与角色映射

### `customer`

可访问页面：

- 登录
- 仪表盘
- 上传包裹
- 包裹
- 订单
- 订单详情

核心动作：

- 上传包裹
- 查看包裹状态
- 申请打包
- 查看订单

### `staff`

可访问页面：

- 登录
- 仪表盘
- 上传包裹
- 包裹
- 订单
- 订单详情
- 任务
- 结算
- 客户
- 客户详情

核心动作：

- 查看全局包裹和订单
- 结算与价格覆盖
- 客户查询

### `operator`

可访问页面：

- 登录
- 仪表盘
- 包裹
- 订单
- 订单详情
- 任务

核心动作：

- 标记到仓
- 处理通知
- 开始任务
- 填写重量
- 完成打包

## 5. 当前页面能力边界

以下内容需要注意：

- 登录是 mock，不是正式鉴权
- `Dashboard.vue` 仍偏静态
- `CustomerDetail.vue` 功能较轻，仅覆盖最基本展示
- 部分流程在页面上已具备入口，但后端状态仍以当前实现为准
- 更完整的发货、异常、取消、待付款等链路仍属于后续扩展方向
