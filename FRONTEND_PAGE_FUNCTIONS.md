# TMS 前端页面功能说明

本文描述当前前端实际能力，并把后续目标页面单独列出。页面入口不等于后端动作一定成功；前端权限仅用于 UI 收敛，后端 RBAC 是最终安全边界。

## 1. 当前页面总览

| 页面 | 路由 | 角色 | 当前用途 |
|---|---|---|---|
| 登录 | `/login` | 公共 | 使用后端演示账号登录 |
| 仪表盘 | `/` | 已登录用户 | 查看按角色加载的包裹、订单、任务概览 |
| 上传包裹 | `/upload` | `customer` / `staff` | 创建包裹并上传图片 |
| 包裹 | `/parcels` | `customer` / `staff` / `operator` | 查询包裹、到仓操作、申请打包、查看通知 |
| 订单 | `/orders` | `customer` / `staff` / `operator` | 查询订单、筛选、进入详情 |
| 订单详情 | `/orders/:id` | `customer` / `staff` / `operator` | 查看订单、重量信息和后续动作 |
| 任务 | `/tasks` | `staff` / `operator` | 查看打包任务，操作员开始任务 |
| 结算 | `/billing` | `staff` | 计算价格和人工覆盖价格 |
| 客户 | `/customers` | `staff` | 查看客户列表 |
| 客户详情 | `/customers/:id` | `staff` | 查看客户及其关联订单 |

## 2. 公共能力

- Vue 3 + Vue Router 页面导航。
- Pinia 保存当前登录状态。
- 登录使用 FastAPI 的邮箱/密码接口，返回 Bearer Token。
- 路由守卫限制未登录用户和不符合角色的页面访问。
- 导航栏按角色显示页面入口。
- API client 统一添加 Bearer Token。
- 主题切换和基础响应式布局。

当前登录账号是本地演示账号，不是用户注册、企业 SSO 或生产身份系统。Token 存在浏览器 `localStorage`，后续需要补过期处理和统一登出。

## 3. 当前页面功能

### 3.1 登录页 `Login.vue`

当前功能：

- 输入邮箱和密码。
- 调用 `POST /auth/login`。
- 显示客户、员工、操作员演示账号提示。
- 登录成功后进入仪表盘。

不包含：注册、找回密码、二次验证、真实用户管理。

### 3.2 仪表盘 `Dashboard.vue`

当前功能：

- 按角色请求包裹、订单、任务和部分通知数据。
- 显示数量指标、筛选栏和业务列表。
- 客户看到自己的包裹/订单概览。
- 操作员看到任务和打包通知概览。

当前限制：

- 不是完整 BI 仪表盘。
- 统计主要由列表数据计算，缺少后端聚合接口。
- 员工的任务请求与后端权限存在待修复的错位。

### 3.3 上传包裹 `Upload.vue`

当前功能：

- 客户或员工创建包裹。
- 填写客户、运单号、快递公司、品类、物品描述和备注。
- 上传最多 5 张 PNG/JPEG/WebP 图片，单张不超过 5MB。
- 客户角色自动使用自己的客户 ID。

当前限制：

- 图片只保存到演示目录，没有图片元数据、预览 URL、删除和访问授权。
- 没有包裹编辑和批量导入。

### 3.4 包裹页 `Parcels.vue`

客户可执行：

- 查看自己的包裹。
- 按运单号和状态筛选。
- 选择 `ARRIVED` 包裹。
- 填写目的地、渠道、服务和收件人信息。
- 提交打包申请。
- 查看打包相关通知和本地草稿。

员工可执行：

- 查看全局包裹。
- 按客户和状态筛选。
- 将包裹标记为 `ARRIVED`。

操作员可执行：

- 查看包裹。
- 查看和清空打包/待发货通知。

### 3.5 订单页 `Orders.vue`

当前功能：

- 查看订单列表。
- 按订单号、运单号、客户、状态和时间筛选或排序。
- 查看包裹数、实际重量、体积重量和最终价格。
- 进入订单详情。
- 提供开始任务、发货和调整包裹等操作入口。

当前限制：

- 异常处理已独立到 `/exceptions`，不在订单列表直接处理。
- 没有拆单、合单、取消状态或真实运输轨迹和承运商信息。

### 3.6 订单详情 `OrderDetail.vue`

当前功能：

- 查看订单状态和客户信息。
- 查看关联包裹和转运信息。
- 保存实际重量。
- 根据长宽高计算体积重量。
- 操作员完成打包。
- 员工完成发货并通知客户。

当前限制：

- 没有 shipment、运输事件和签收状态。
- 异常登记和解决在独立异常页；没有退款或理赔流程。
- 订单与包裹关系由后端 `order_parcels` 维护，响应仍兼容 `parcel_ids`。

### 3.7 任务页 `Tasks.vue`

当前功能：

- 从 `/tasks` 获取真实任务。
- 按 `TODO`、`IN_PROGRESS`、`DONE` 分栏。
- 操作员开始 `TODO` 任务。
- 跳转订单详情继续处理重量和打包。

当前限制：

- 没有负责人、领取、优先级、截止时间和异常记录。
- 当前一个订单只创建一个打包任务。

### 3.8 结算页 `Billing.vue`

当前功能：

- 查看已完成订单。
- 显示已完成订单的已保存实际重量，并输入单价和额外费用。
- 计算 `actual_weight × rate_per_kg + extra_fee`。
- 人工覆盖最终价格时填写原因。
- 显示服务端自动计价和人工改价审计记录。

当前限制：

- 没有币种、付款状态、退款、账单明细和审批流。

### 3.9 订单异常 `Exceptions.vue`

当前功能：

- 客户登记取消、地址错误或价格争议。
- 操作员登记破损或违禁品；员工可登记全部类型。
- 所有角色查看自己有权限的异常；员工填写解决说明并将异常从 `OPEN` 处理为 `RESOLVED`。
- 未解决异常会由后端阻止发货和价格操作。

### 3.10 客户页面 `Customers.vue` / `CustomerDetail.vue`

当前功能：

- 员工搜索和查看客户。
- 查看客户关联订单。

当前限制：

- 没有新增、编辑客户。
- 客户详情缺少地址、包裹统计、异常记录和账务摘要。

## 4. 角色页面映射

### `customer`

可访问：仪表盘、上传包裹、包裹、订单、订单详情、异常。

核心流程：创建包裹 → 查看状态 → 选择到仓包裹 → 申请打包 → 查看订单。

### `staff`

可访问：仪表盘、上传包裹、包裹、订单、订单详情、任务、结算、异常、客户。

核心流程：确认到仓 → 管理订单 → 处理重量/价格 → 确认发货 → 查看客户。

### `operator`

可访问：仪表盘、包裹、订单、订单详情、任务、异常。

核心流程：查看任务 → 开始任务 → 处理打包 → 记录重量 → 完成打包。

## 5. 目标页面扩展

以下页面是后续规划，不代表当前已经实现：

| 阶段 | 目标页面 | 主要能力 |
|---|---|---|
| TMS 核心 | 包裹详情/文件 | 图片预览、包裹事件、异常记录 |
| TMS 核心 | 订单拆分/合单 | 使用 `order_parcels` 管理包裹关系 |
| TMS 核心 | 出库单/运输详情 | 承运商、转运单号、运输事件、签收 |
| 仓库运营 | 我的任务 | 任务领取、优先级、装箱记录、复核 |
| 结算 | 账单/改价审批 | 计价明细、付款和审批 |
| AI | 运营助手 | 查询订单、包裹、任务和运输事件 |
| AI | 异常分类 | 分类、严重程度、人工复核建议 |
| AI | 延误分析 | 延误概率、原因解释和运营建议 |

## 6. 文档边界

当前可写入简历的页面能力限于包裹、集运订单、仓库任务、基础计价和角色权限。运输事件、AI Agent、RAG、预测、付款和真实承运商接入，只有在相应代码、测试和运行结果完成后才能加入项目经历。

整体技术路线见 [ARCHITECTURE_ROADMAP.md](ARCHITECTURE_ROADMAP.md)。
