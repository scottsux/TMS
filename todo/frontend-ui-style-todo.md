# 前端 UI 风格整体优化 TODO

## 本轮已完成

- 已完成全局视觉基线统一：颜色、字号、按钮、表单、表格、状态 badge、导航外壳
- 已完成高频页面重构：`Orders.vue`、`Parcels.vue`、`OrderDetail.vue`、`Billing.vue`、`Tasks.vue`
- 已完成 `Dashboard.vue` 角色化工作台改造，并接入现有真实接口数据
- 已完成 `Customers.vue`、`CustomerDetail.vue` 风格统一
- 已完成登录页、未登录壳层、角色切换页和顶部栏的主要布局修正
- 已完成主要小屏适配收口，包括弹窗滚动、顶部栏收缩、按钮不竖排、工具条换行

## 当前仍建议优化

### 1. Dashboard 数据深度仍可继续增强

- 当前仪表盘已接真实数据，但仍受现有接口字段限制
- `operator` 的“今日完成”目前保守降级为“已完成任务数”
- 如果后端后续补稳定完成时间字段，可以把今日维度做准确

### 2. Customers / CustomerDetail 业务信息仍偏轻

- 当前已完成样式统一
- 但业务层面仍是轻量展示页
- 后续可补客户关联包裹、最近通知、订单状态分布等更完整信息

### 3. Dashboard / Customer 页仍可进一步减少局部内联样式

- 当前主体风格已统一
- 但个别页面仍存在少量局部内联布局样式
- 若后续继续优化，可再抽成更纯粹的共享布局类

## 响应式复查结论

本轮已针对以下宽度进行实现收口：

- `390px`
- `768px`
- `1280px`
- `1440px`

重点已处理：

- 顶部栏在小屏下不再明显挤压
- 未登录页和切换角色页不再出现壳层布局冲突
- 弹窗在小屏下可滚动
- 筛选区在小屏下可换行
- 按钮文字不再出现竖排问题

当前未发现必须阻塞上线演示的前端样式问题。

## 后续非阻塞优化建议

1. 继续抽出真正的 `FilterBar` 组件，替代当前共享 `.toolbar` 模式
2. 为 `Dashboard` 增加更清晰的“最近异常 / 最近通知”区块
3. 为 `Customers` 和 `CustomerDetail` 增加更丰富的业务摘要
4. 如果后端后续扩状态模型，再同步扩展状态色、流程动作和仪表盘统计
